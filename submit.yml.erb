<%-
  base_slurm_args = []
  slurm_args = base_slurm_args + resource.split
-%>
---
batch_connect:
  template: "basic"
  conn_params:
    - csrf_token # Not sure if needed
script:
  native:
    <%- slurm_args.each do |arg| %>
    - "<%= arg %>"
    <%- end %>
